# Deep Research System - Environment Configuration
# For Strix Halo (Ryzen AI Max) - RDNA 3.5 / gfx1151

# =============================================================================
# GPU CONFIGURATION (CRITICAL for Strix Halo)
# =============================================================================
# Find your GIDs with: getent group video | cut -d: -f3
VIDEO_GID=44
# Find your GIDs with: getent group render | cut -d: -f3  
RENDER_GID=991

# Strix Halo GPU architecture override
HSA_OVERRIDE_GFX_VERSION=11.5.1

# =============================================================================
# OLLAMA (Inference Engine)
# =============================================================================
OLLAMA_HOST=http://ollama:11434
OLLAMA_MODEL=gpt-oss:20b

# =============================================================================
# BACKEND
# =============================================================================
BACKEND_PORT=8000
BACKEND_HOST=0.0.0.0
DATABASE_PATH=/data/research.db

# LLM Configuration
LLM_BASE_URL=http://ollama:11434/v1
LLM_MODEL=gpt-oss:20b
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4096

# Research Safeguards
MAX_ITERATIONS=10
MAX_RESEARCH_TIME_MINUTES=30
TOKEN_BUDGET=500000

# =============================================================================
# FRONTEND
# =============================================================================
FRONTEND_PORT=5173
VITE_API_URL=http://localhost:8000

# =============================================================================
# SEARCH (Optional - for Source Finder)
# =============================================================================
# TAVILY_API_KEY=your_tavily_key_here
# Or use DuckDuckGo (no key required)
SEARCH_PROVIDER=duckduckgo
